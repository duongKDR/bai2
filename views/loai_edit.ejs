<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />

    <title>Document</title>
  </head>
  <body>
    <div class="container">
      <form
        class="col-6 border border-dark rounded p-3 m-auto"
        action="/loai/update/<%=sp.id  %>"
        method="POST"
      >
        <div class="form-group mb-3">
          <label for="name">Tên </label>
          <input
            class="form-control bg-info"
            value="<%=sp.name  %> "
            name="name"
            id="name"
          />
        </div>
        <div class="form-group mb-3">
          <label for="content">Nội dung</label>
          <input
            class="form-control bg-primary"
            value="<%=sp.content  %>"
            name="content"
            id="content"
          />
        </div>
        <div class="form-group mb-3">
          <label for="image">Image</label>
          <input
            class="form-control bg-primary"
            value="<%=sp.image  %>"
            name="image"
            id="image"
          />
        </div>
        <div class="form-group mb-3">
          <label for="VND">VND</label>
          <input
            class="form-control bg-primary"
            value="<%=sp.VND  %>"
            name="VND"
            id="VND"
          />
        </div>
        <div class="form-group mb-3">
          <label for="up_date">up date</label>
          <input class="form-control bg-primary" name="up_date" id="up_date" />
        </div>
        <div class="form-group mb-3">
          <label>Ẩn hiện</label>
          <input type="radio" name="hidden" value="1" <%=(sp.hidden==1)?
          "checked" :"" %> /> Hiện &nbsp; &nbsp; <input type="radio"
          name="hidden" value="0" <%=(sp.hidden==0)? "checked" :"" %>/> Ẩn
        </div>
        <div class="form-group mt-3">
          <button type="submit" class="btn btn-dark px-4 py-2 text-white">
            LƯU DATABASE
          </button>
        </div>
      </form>
    </div>
  </body>
</html>
